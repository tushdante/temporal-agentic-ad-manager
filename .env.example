# Temporal Cloud (set all three for Cloud, or omit all for local dev server)
TEMPORAL_ADDRESS=your-namespace.tmprl.cloud:7233
TEMPORAL_NAMESPACE=your-namespace
TEMPORAL_API_KEY=your-temporal-api-key

# Local dev server: just run `temporal server start-dev`
# No .env needed — defaults to localhost:7233, namespace "default"
